#include "main.h"

/**
 * _strlen - returns length of a string
 * @s: pointer to the string
 *
 * Return: len of string
 */

int _strlen(char *s)
{
int length;

while (s[length != 0])
{
length++;
}
return length;
}
